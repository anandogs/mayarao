import { atom } from 'nanostores';

const allItemns = {
    isSoloGroupCreationsOpen: atom(false),
}

export const isSoloGroupCreationsOpen = atom(false)
export const isTheatreInEducationOpen = atom(false)